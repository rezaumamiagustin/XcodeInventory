<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rezaumami.xcodeinventory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.LowStock"></activity>
        <activity android:name=".ui.AllTransaction" />
        <activity android:name=".ui.EditTransaction" />
        <activity android:name=".ui.AddProduct" />
        <activity android:name=".ui.SelectProduct" />
        <activity android:name=".ui.AddTransaction" />
        <activity android:name=".ui.EditProductsActivity" />
        <activity android:name=".ui.ProductsActivity" />
        <activity android:name=".ui.DashboardActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>